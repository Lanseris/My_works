Базу данных скинул, чтобы не нужно было заполнять какими-то данными самим, а так,
 я использовал подход CodeFirst при работе с Entity Framevork, по этому, при запуске программы в первый раз,
 если на сервере не будет нужной базы, то она должна её создать, она будет пытаться конектится к SQLEXPRESS (Скрин 1)
эти настройки находятся в файле App.config , код для заполнения и простого просмотра данных через программу здесь (Скрин 2).

Сразу скажу, задание я НЕ выполнил, причины просты, ни с Entity ни с патерном MWP раньше не работал-> пытался их изучить
и если с Entity ещё всё более менее было понятно и относительно просто, то с пониманием MWP возникли трудности
пока пробовал его понять, попытался написать программу по заданию, но в итоге практически ничего из задания не выполнено
есть лишь форма, на которой выводится список всех заданий, описание нескольких событий и кнопка,
 позволяющая внести изменения в данные, которые обрабатывает программа (название и описание задания) и то,
 обновление базы не производится, т к из-за не понимания патерна я пока не знаю куда их пихнуть.

как-то так.

В любом случае, ЭТО я буду пытаться дорабатывать, как минимум для себя, т к знание MWP мне не помешает)